

# biliAntiShadowban 安装说明

**B站评论防和谐** · 作者 [@依然匹萨吧](https://space.bilibili.com/6297797)

## 安装包

| 文件 | 适用浏览器 |
|---|---|
| `biliAntiShadowban-chrome.zip` | Chrome / Edge / 其他 Chromium 内核（360、QQ、Brave、Opera 等） |
| `biliAntiShadowban-firefox.xpi` | Firefox |
| `biliAntiShadowban-firefox.zip` | 同上，提交 AMO 审核时用这个 |
| `chrome/`、`firefox/` 目录 | 已解压版本，开发调试直接加载 |

---

## Chrome / Edge 安装

Chrome 和 Edge 出于安全策略**不允许直接双击 zip 安装**：

1. 把 `biliAntiShadowban-chrome.zip` **解压**到一个目录
   > ⚠️ 解压后该文件夹不能删除或移动，否则扩展失效
2. 打开 `chrome://extensions`（Edge 为 `edge://extensions`）
3. 打开 **「开发者模式」** 和 **「允许来自其他应用商店的扩展」**
4. 点 **「加载已解压的扩展程序」**，选择第 1 步的文件夹
5. 出现「B站评论防和谐」即成功

## Firefox 安装

**临时安装**（重启失效）：`about:debugging#/runtime/this-firefox` →「临时载入附加组件」→ 选 `.xpi`

**永久安装**：Firefox 正式版强制签名。可用 Developer Edition / Nightly / ESR，在 `about:config` 把
`xpinstall.signatures.required` 设为 `false` 后拖入 `.xpi`；或提交 [AMO](https://addons.mozilla.org/developers/) 签名。

---

## 使用

登录 B 站，打开任意**视频 / 动态 / 专栏**页，评论发送按钮旁会出现「敏感检测」按钮。

面板分为两块，**性质完全不同，请勿混淆**：

### ①「本地推测 · 非官方结果」（灰色标签）
扩展内置启发式词库的判断，精确到第几个字。**B站审核规则保密、无公开接口，发送前不可能得到官方结论**，所以这块只是推测，可能误报漏报。

### ②「官方判定核验」（蓝色标签）★
真正来自官方的结果，**零写入**：扩展**不会代你发送、也不会制造任何测试评论**。

你像平常一样点击B站的发送按钮后，扩展会：
1. 旁路读取这条评论的官方返回码（评论数量增量为 0）
2. 数秒后用只读接口比对游客视角，判断是否被 ShadowBan

结果以浮层自动弹在右下角：

| 结果 | 含义 |
|---|---|
| ✕ 官方拒绝发送 `12016` | 命中顶级敏感词库，**全站通用，可信度最高** |
| ⚠ 仅自己可见（ShadowBan） | 登录可见、游客不可见 |
| ✕ 已被系统删除 | 发出后被判违规 |
| ✓ 他人可见，正常 | 通过 |

> ⚠️ 判定只能在**发送之后**取得，无法在发送前给出官方结论。
> 审核存在「秋后算账」（初始正常、之后才被隐藏），建议过一段时间用评论旁的徽标复查。
> 本功能不读取任何 Cookie，**不需要 `cookies` 权限**。

### ③ 和谐处理
需先在设置页勾选风险确认才会显示。同音字（你好→泥豪）/ 零宽字符（视觉不变）/ 混合三种。

> ⚠️ 本质是规避审核，**不会让违规内容变得合规**；「刻意规避审核」本身即可能违规并加重处罚。风险自负。

### ④ 已发评论可见性标识
自己的评论后显示：✓他人可见 / ⚠仅自己可见 / ?无法确定。

---

## 常见问题

**按钮没出现？** 刷新页面、确认已登录、确认是视频/动态/专栏页；评论区异步加载，等 1–2 秒。

**可见性一直「无法确定」？** 默认只探测前 2 页；也可能触发了限流熔断（点扩展图标可看状态，冷却 10 分钟）。

**发送后没弹出判定浮层？** 确认设置页「官方判定核验」已开启并刷新页面；该功能需要读取页面自身的发送请求，刷新后才会挂载。

**会不会导致账号被风控？** 扩展**不发起任何写请求**，不会替你发评论。读操作已做全局串行 + 令牌桶 + 随机抖动 + 自动熔断。
不想发任何请求就关掉「可见性检测」和「官方判定核验」，扩展即成为纯离线工具。

**会上传我的评论吗？** 不会。本地分析全在本机，扩展无任何自建服务端。详见 `PRIVACY.md`。

---

## 从源码构建

```bash
npm install
node build.mjs     # 产出 dist/chrome 与 dist/firefox
npm run pack       # 另外产出 zip
npm test           # 检测 / 和谐 / 拦截器零写入 / 限流 四组测试
```

---

安装即视为同意 `LEGAL.md` 全部条款。本扩展为非官方第三方工具，与哔哩哔哩无任何隶属关系。

© [@依然匹萨吧](https://space.bilibili.com/6297797)



